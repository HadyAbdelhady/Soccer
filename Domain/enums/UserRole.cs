namespace Infra.enums
{
    /// <summary>
    /// System roles: Team (login as team), Admin, Viewer.
    /// </summary>
    public enum UserRole
    {
        Team,
        Admin,
        Viewer
    }
}
